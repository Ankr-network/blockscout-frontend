{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "lerna clean && rm -rf node_modules",
    "prepare": "husky install",
    "release": "lerna version --yes --conventional-commits --message \"chore(release): %s\""
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^16.1.0",
    "@commitlint/config-conventional": "^16.0.0",
    "eslint-plugin-import": "^2.25.3",
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  },
  "resolutions": {
    "react-error-overlay": "6.0.9"
  }
}
