{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "lerna clean && rm -rf node_modules",
    "prepare": "husky install"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "husky": "^7.0.4"
  }
}
