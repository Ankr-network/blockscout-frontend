{
  "name": "e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "yarn clean",
    "test": "cross-env TEST_ENV=stage yarn playwright test",
    "pretest:healthCheck": "yarn clean",
    "test:healthCheck": "cross-env TEST_ENV=stage yarn playwright test --project=healthCheck",
    "clean": "rimraf ./tmp && rimraf ./test-results && rimraf ./dist/metamask"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.19.2",
    "@types/chroma-js": "^2.1.3",
    "bignumber.js": "^9.0.2",
    "chroma-js": "^2.4.2",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "numeral": "^2.0.6",
    "rimraf": "^3.0.2"
  }
}
