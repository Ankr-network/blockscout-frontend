export enum ChainType {
  Devnet = 'devnet',
  Mainnet = 'mainnet',
  Testnet = 'testnet',
}

export enum SortType {
  Name = 'name',
  Usage = 'usage',
}

export enum StatsTimeframe {
  DAY,
  WEEK,
  MONTH,
}
