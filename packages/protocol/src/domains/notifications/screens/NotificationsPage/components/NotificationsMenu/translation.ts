import { Locale } from 'modules/i18n';

export const notificationsMenuTranslation = {
  [Locale.en]: {
    markRead: 'Mark all as read',
    removeAll: 'Remove all',
    settings: 'Settings',
  },
};
