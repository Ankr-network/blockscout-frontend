import { makeStyles } from 'tss-react/mui';

import { CONTAINER_STYLES } from 'modules/layout/components/DefautLayout';

export const useChainItemStyles = makeStyles()(() => ({
  chainItemWrapper: CONTAINER_STYLES,
}));
