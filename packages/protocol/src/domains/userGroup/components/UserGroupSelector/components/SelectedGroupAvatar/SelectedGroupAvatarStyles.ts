import { makeStyles } from 'tss-react/mui';

export const useSelectedGroupAvatarStyles = makeStyles()(() => ({
  avatar: {
    width: 40,
    height: 40,
  },
  personalIcon: {
    fontSize: 28,
  },
}));
