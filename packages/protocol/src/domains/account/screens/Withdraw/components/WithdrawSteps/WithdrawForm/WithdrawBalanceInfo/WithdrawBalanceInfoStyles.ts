import { Theme } from '@material-ui/core';
import { makeStyles } from '@material-ui/core/styles';

export const useWithdrawBalanceInfoStyles = makeStyles<Theme>(() => ({
  balance: {
    fontWeight: 400,
  },
}));
