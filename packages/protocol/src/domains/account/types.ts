export enum BalanceStatus {
  GREEN,
  RED,
  YELLOW,
}

export enum AccountType {
  PAYG_ACTIVE,
  PAYG_INACTIVE,
  PAYG_UNKNOWN,
  PAYG_UNKNOWN_WITH_BALANCE,
  PAYG_WARNING,
  PAYG_WARNING_ZERO,
  PREMIUM,
  PREMIUM_EXPIRED,
}

export enum Currency {
  ANKR,
  CREDIT,
}
