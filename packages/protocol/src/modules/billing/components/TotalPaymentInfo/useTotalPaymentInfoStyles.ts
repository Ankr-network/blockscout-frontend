import { makeStyles } from 'tss-react/mui';

export const useTotalPaymentInfoStyles = makeStyles()(() => ({
  root: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
}));
