import { makeStyles } from '@material-ui/styles';

export const useH1TagStyles = makeStyles(() => ({
  title: {
    visibility: 'hidden',
    lineHeight: 0,
    maxWidth: '100%',
    overflow: 'hidden',
  },
}));
