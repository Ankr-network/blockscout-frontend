import { Box } from '@material-ui/core';

import { Container } from 'uiKit/Container';

export const DevPage = (): JSX.Element => {
  return (
    <Box py={6}>
      <Container maxWidth={800}>Dev page</Container>
    </Box>
  );
};
