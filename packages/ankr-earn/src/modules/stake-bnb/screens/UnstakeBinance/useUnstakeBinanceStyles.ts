import { makeStyles } from '@material-ui/core';

export const useUnstakeBinanceStyles = makeStyles(() => ({
  infoLabel: {
    fontSize: 14,
    fontWeight: 700,
  },

  infoValue: {
    fontSize: 16,
    fontWeight: 700,
  },
}));
