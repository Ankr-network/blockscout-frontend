import { makeStyles } from '@material-ui/core/styles';

export const useUnstakeFantomStyles = makeStyles(theme => ({
  willGetLabel: {
    fontWeight: 'bold',
    fontSize: 14,
  },
  willGetValue: {
    fontWeight: 'bold',
    fontSize: 16,
  },
}));
