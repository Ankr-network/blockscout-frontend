import { makeStyles } from '@material-ui/core/styles';

export const useUnstakeFantomStyles = makeStyles(theme => ({
  formFooter: {
    fontSize: 14,
  },

  label: {
    fontWeight: 'bold',
    fontSize: 'inherit',
  },

  value: {
    fontSize: 'inherit',
  },

  totalValue: {
    fontWeight: 'bold',
    fontSize: 16,
  },
}));
