import { makeStyles } from '@material-ui/core/styles';

export const useUnstakeFantomStyles = makeStyles(() => ({
  formFooter: {
    fontSize: 14,
  },

  label: {
    fontSize: 'inherit',
    fontWeight: 'bold',
  },

  value: {
    fontSize: 'inherit',
  },

  totalValue: {
    fontSize: 16,
    fontWeight: 'bold',
  },
}));
